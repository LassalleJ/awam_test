{% extends 'base.html.twig' %}

{% block title %}ExchangeRate{% endblock %}

{% block body %}
    <h1>ExchangeRate</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ exchange_rate.id }}</td>
            </tr>
            <tr>
                <th>Rate</th>
                <td>{{ exchange_rate.rate }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_exchange_rate_index') }}">back to list</a>

    <a href="{{ path('app_exchange_rate_edit', {'id': exchange_rate.id}) }}">edit</a>

    {{ include('exchange_rate/_delete_form.html.twig') }}
{% endblock %}
